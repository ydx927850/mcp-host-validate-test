[project]
name = "McpServerTemplate"
version = "0.1.0"
description = "T4: dependencies 无 mcp"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "requests>=2.0",
    "httpx>=0.27.0",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
