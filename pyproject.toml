[project]
name = "McpServerTemplate"
version = "0.1.0"
description = "T3: pyproject 无 dependencies"
readme = "README.md"
requires-python = ">=3.10"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
